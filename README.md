# Relationship CI PCI Visualizer

Run the quick instructions in the project root:

```
python -m venv venv
source venv/bin/activate
pip install -r requirements.txt
python app.py
```

Open `http://127.0.0.1:5000/static/index.html` in a browser.
